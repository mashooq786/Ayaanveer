#!/usr/bin/python
import sys
try:
    import veerjan
    if hasattr(veerjan, 'main'):
        veerjan.main()
    elif hasattr(veerjan, 'menu'):
        veerjan.menu()
    else:
        for func in dir(veerjan):
            if not func.startswith('_'):
                print(func)
except Exception as e:
    print(f"Error loading module: {e}")
